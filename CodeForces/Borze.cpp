#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    while(1){
    set<int>a;

    for(int i = 0 ; n/10>0;i++){
        s.insert(n%10);
    }
    }
    return 0;
}