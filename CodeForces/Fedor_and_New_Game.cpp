#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, m, k, x;
    cin >> n >> m >> k;
    int a[m + 1];
    for (int i = 0; i < m; i++)
        cin >> a[i];
    cin >> x;
    return 0;
}