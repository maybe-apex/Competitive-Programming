#include <bits/stdc++.h>

using namespace std;

int main()
{
    int h, w;
    cin >> h >> w;
    int r[h], c[w];
    for (int i = 0; i < h; i++)
        cin >> r[i];
    for (int i = 0; i < w; i++)
        cin >> c[i];

    return 0;
}