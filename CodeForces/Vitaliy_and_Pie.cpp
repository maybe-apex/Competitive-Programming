#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n, j = 0;
    cin >> n;
    vector<int> a, b;
    for (int i = i; i <= 2 * (n - 2); i++)
    {
        int x, y;
        cin >> x >> y;
        a.push_back(x);
        b.push_back(y);
    }
    for (int i = 0; i < b.size(); i++)
    {
        if (find(a.begin(), a.end(), b[i]))
    }
    return 0;
}