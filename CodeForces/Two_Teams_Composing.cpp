#include <iostream>

using namespace std;

int main()
{

    int t;
    cin >> t;
    while (t--)
    {
        int n, a[10000];
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
    }

    return 0;
}